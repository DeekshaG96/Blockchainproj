import express from "express";
import { createServer as createViteServer } from "vite";
import path from "path";
import { fileURLToPath } from "url";

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

async function startServer() {
  const app = express();
  const PORT = 3000;

  app.use(express.json());

  // API Route for Anomaly Detection (Mock/Simulation)
  app.post("/api/detect-fraud", (req, res) => {
    const { votes } = req.body;
    // Simple mock logic for demonstration
    // In a real app, this would call a Python service or complex ML model
    const anomalies = votes.filter((v: any) => v.pattern === "suspicious");
    res.json({
      status: "success",
      anomalyCount: anomalies.length,
      isCompromised: anomalies.length > 5,
      timestamp: new Date().toISOString()
    });
  });

  // Vite middleware for development
  if (process.env.NODE_ENV !== "production") {
    const vite = await createViteServer({
      server: { middlewareMode: true },
      appType: "spa",
    });
    app.use(vite.middlewares);
  } else {
    const distPath = path.join(process.cwd(), "dist");
    app.use(express.static(distPath));
    app.get("*", (req, res) => {
      res.sendFile(path.join(distPath, "index.html"));
    });
  }

  app.listen(PORT, "0.0.0.0", () => {
    console.log(`Server running on http://localhost:${PORT}`);
  });
}

startServer();
